Use sample-applications.csv for batch uploads.

Use sample-applications-batch-test-10.csv for a larger batch test set with a mix of:
- expected passes
- casing-based review cases
- intentional failures for ABV, net contents, bottler, class/type, and country-of-origin checks
- ten corresponding PNG label files are included for that CSV, one image per row

The image_filename column must match the uploaded image names exactly.
You can create matching demo images with any label artwork, or use generated mock labels that contain the listed fields.
